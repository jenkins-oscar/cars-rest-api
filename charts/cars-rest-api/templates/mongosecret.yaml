apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-auth
  labels:
    draft: {{ default "draft-app" .Values.draft }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
type: Opaque
data:
  username: mongoadmin
  mongodb-username: mongoadmin
  mongodb-password: Password1
  password: Password1
  rootpassword: Password1
  mongodb-root-password: Password1