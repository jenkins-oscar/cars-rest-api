buildPack: javascript
pipelineConfig:
  pipelines:
    overrides:
    - pipeline: release    
      stage: promote
      steps:
        - name: helm-release
          command: jx
          args:
            - step
            - helm
            - apply
            - --name
            - app-name
            - --namespace 
            - jx-staging
          dir: /workspace/source/charts/cars-rest-api